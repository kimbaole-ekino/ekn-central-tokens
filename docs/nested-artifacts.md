# Nested artifacts

One Theme Group produces flat CSS paths. Several Theme Groups produce paths ordered by Theme Group appearance, for example `brand/dark.css`. These paths become explicit package exports. Collision checks use the final case-insensitive path.

See the [artifact contract](artifact-contract.md) for selectors and the [project package guide](project-packages.md) for imports.
