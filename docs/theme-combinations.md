# Theme combinations

Central gets every Theme permutation from `@ekinotech/design-token-validator`. It does not define Theme combinations in configuration. Later enabled Sets win at one token path; source Sets resolve aliases but do not emit tokens.

The same resolved graph feeds Style Dictionary and Storybook. Any unresolved diagnostic stops the build.
